pipeline {
    agent any

    stages {

        stage('Clone Code') {
            steps {
                git(
                    branch: 'main',
                    url: 'https://github.com/tallurisravya6618-pixel/Three-dev-aws.git'
                )
            }
        }

        stage('Install Backend Dependencies') {
            steps {
                sh 'npm install'
            }
        }
    }
}
